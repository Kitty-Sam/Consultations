import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    rootWrapper: { flex: 1 },
    wrapper: {
        display: 'flex',
        alignItems: 'center',
        flex: 1,
    },
    title: {
        marginVertical: 28,
        marginHorizontal: 14,
        fontSize: 16,
    },
});
